@using BlazorMQTTTestingWASM.Models
@using MQTTnet
@inject MQTTnet.ClientLib.MqttService MqttService


<MudCard Outlined="true">
    <MudCardContent>
        <MudGrid Spacing="10">
            <MudItem xs="6">
                <MudText Typo="Typo.h6"
                         Align="MudBlazor.Align.Left">
                    Canopy Humidity: @tempHum.CanopyHum %
                </MudText>
                <MudToggleIconButton @bind-Toggled="_TurnCanopyHumAlarmOnOff"
                                     Icon="@Icons.Material.Filled.AlarmOff"
                                     Color="MudBlazor.Color.Warning"
                                     ToggledIcon="@Icons.Material.Filled.AlarmOn"
                                     ToggledColor="MudBlazor.Color.Tertiary"
                                     title="@(_TurnCanopyHumAlarmOnOff ? "On" : "Off")" />
                Canopy Humidity Alarms are @(_TurnCanopyHumAlarmOnOff ? "On" : "Off")

                <span>
                    @if (_TurnCanopyHumAlarmOnOff)
                    {
                        <MudNumericField @bind-Value="tempHum.CanopyHumAlarmLow"
                                         Label="Low Humidity Alarm"
                                         Variant="Variant.Outlined"
                                         Min="0" Max="100" />

                        <MudNumericField @bind-Value="tempHum.CanopyHumAlarmHigh"
                                         Label="High Humidity Alarm"
                                         Variant="Variant.Outlined"
                                         Min="0" Max="100" />
                    }
                </span>
            </MudItem>

            <MudItem xs="6">
                <MudText Typo="Typo.h6"
                         Align="MudBlazor.Align.Left">
                    Canopy Temperature: @tempHum.CanopyTemp °C
                </MudText>
                <MudToggleIconButton @bind-Toggled="_TurnCanopyTempAlarmOnOff"
                                     Icon="@Icons.Material.Filled.AlarmOff"
                                     Color="MudBlazor.Color.Warning"
                                     ToggledIcon="@Icons.Material.Filled.AlarmOn"
                                     ToggledColor="MudBlazor.Color.Tertiary"
                                     title="@(_TurnCanopyTempAlarmOnOff ? "On" : "Off")" />
                Canopy Temperature Alarms are @(_TurnCanopyTempAlarmOnOff ? "On" : "Off")

                @if (_TurnCanopyTempAlarmOnOff)
                {
                    <MudNumericField @bind-Value="tempHum.CanopyTempAlarmLow"
                                     Label="Low Temperature Alarm"
                                     Variant="Variant.Outlined"
                                     Min="0" Max="100" />

                    <MudNumericField @bind-Value="tempHum.CanopyTempAlarmHigh"
                                     Label="High Temperature Alarm"
                                     Variant="Variant.Outlined"
                                     Min="15" Max="30" />
                }
            </MudItem>
        </MudGrid>
    </MudCardContent>
</MudCard>

@code {

    public bool _TurnCanopyHumAlarmOnOff { get; set; }

    public bool _TurnCanopyTempAlarmOnOff { get; set; }
}

<MudCard Outlined="true">
    <MudCardContent>
        <MudGrid Spacing="10">
            <MudItem xs="6">
                <MudText Typo="Typo.h6"
                         Align="MudBlazor.Align.Left">
                    Tank Temperature: @tempHum.TankTemp °C
                </MudText>
                <MudToggleIconButton @bind-Toggled="_TurnTankTempAlarmOnOff"
                                     Icon="@Icons.Material.Filled.AlarmOff"
                                     Color="MudBlazor.Color.Warning"
                                     ToggledIcon="@Icons.Material.Filled.AlarmOn"
                                     ToggledColor="MudBlazor.Color.Tertiary"
                                     title="@(_TurnTankTempAlarmOnOff ? "On" : "Off")" />
                Tank Temperature Alarms are @(_TurnTankTempAlarmOnOff ? "On" : "Off")

                <span>
                    @if (_TurnTankTempAlarmOnOff)
                    {
                        <MudNumericField @bind-Value="tempHum.TankTempAlarmLow"
                                         Label="Low Humidity Alarm"
                                         Variant="Variant.Outlined"
                                         Min="15" Max="30" />

                        <MudNumericField @bind-Value="tempHum.TankTempAlarmHigh"
                                         Label="High Humidity Alarm"
                                         Variant="Variant.Outlined"
                                         Min="15" Max="30" />
                    }
                </span>
            </MudItem>

            <MudItem xs="6">
                <MudText Typo="Typo.h6"
                         Align="MudBlazor.Align.Left">
                    Sump Temperature: @tempHum.SumpTemp °C
                </MudText>
                <MudToggleIconButton @bind-Toggled="_TurnSumpTempAlarmOnOff"
                                     Icon="@Icons.Material.Filled.AlarmOff"
                                     Color="MudBlazor.Color.Warning"
                                     ToggledIcon="@Icons.Material.Filled.AlarmOn"
                                     ToggledColor="MudBlazor.Color.Tertiary"
                                     title="@(_TurnSumpTempAlarmOnOff ? "On" : "Off")" />
                Canopy Temperature Alarms are @(_TurnSumpTempAlarmOnOff ? "On" : "Off")

                @if (_TurnSumpTempAlarmOnOff)
                {
                    <MudNumericField @bind-Value="tempHum.SumpTempAlarmLow"
                                     Label="Low Temperature Alarm"
                                     Variant="Variant.Outlined"
                                     Min="0" Max="100" />

                    <MudNumericField @bind-Value="tempHum.SumpTempAlarmHigh"
                                     Label="High Temperature Alarm"
                                     Variant="Variant.Outlined"
                                     Min="15" Max="30" />
                }
            </MudItem>
        </MudGrid>
    </MudCardContent>
</MudCard>

@code {

    public bool _TurnTankTempAlarmOnOff { get; set; }

    public bool _TurnSumpTempAlarmOnOff { get; set; }

    [Parameter]
    public int systemID { get; set; } = 0;
    [Parameter]
    public int basestationID { get; set; } = 0;
    [Parameter]
    public int nodeID { get; set; } = 0;

    TempHumDevice tempHum;

    private KeyValuePair<int, float?> windDirections;

    protected override async Task OnInitializedAsync()
    {
        this.tempHum = new TempHumDevice(MqttService, this.systemID, this.basestationID, this.nodeID);

        if (MqttService.IsConnected())
        {
            MqttService.MessageReceived += MqttClient_MessageReceived;
        }
    }

    private void MqttClient_MessageReceived(object sender, MqttApplicationMessageReceivedEventArgs e)
    {
        base.StateHasChanged();
    }
}
